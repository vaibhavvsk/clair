# Clair CLI binaries (large, download from GitHub releases)
clairctl-linux-amd64
clair

# Scan output reports
clair-report-*.json
clair-report-*.txt
clair-report-*.text
clair-report-*.xml


# Vulnerability mirror dumps (large, transfer separately)
vuln-mirror.dump
vuln-db.gz
